node_modules/
dist/
.wrangler/

# Local configuration and credentials
.env
.env.*
!.env.example
.dev.vars
.seed.sql

# The real roster, in plaintext. THIS REPO IS PUBLIC: the friends group lives in
# the USERS_JSON secret only (.env locally, a GitHub Actions secret in CI), and
# `users.example.json` is the throwaway stand-in that ships instead.
users.json
users.md

# Test artifacts
playwright-report/
test-results/
coverage/

# Terraform
iac/.terraform/
iac/.terraform.lock.hcl
*.tfstate
*.tfstate.*
iac/*.tfvars

# Generated by Wrangler
worker-configuration.d.ts

# Agent scratch space (issue dumps, handoff notes) — see docs/AGENT-WORKFLOW.md
.context/

# Human scratchpad
docs/scratchpad/

# Agent worktrees: a second checkout of this repo INSIDE the working tree, which
# `prettier --check .` and jscpd would otherwise walk — every file twice is 50%
# duplication and a red gate. The rest of `.claude/` is config and stays tracked.
.claude/worktrees/
